#Lancement du backend :
ce placer dans le chemin du dossier backend et exécuter cette commande dans le terminal
`uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload`

#Lancement du frontend :
ce placer dans le chemin du dossier frontend et exécuter cette commande dans le terminal
`npm run dev`